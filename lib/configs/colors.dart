import 'package:flutter/material.dart';
import 'package:queued/helpers/color_map.dart';

class AppColors {
  static final primary = ColorMap.createMaterialColor(Color(0xFFDD7373));
  static final secondary = ColorMap.createMaterialColor(Color(0xFF312C50));
  static final white = ColorMap.createMaterialColor(Colors.white);
}
